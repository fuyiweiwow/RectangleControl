<Window x:Class="RectangleControl.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:p="http://prismlibrary.com/"
        xmlns:b ="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:slc="clr-namespace:RectangleControl.Selectors"
        xmlns:local="clr-namespace:RectangleControl"
        mc:Ignorable="d"
        p:ViewModelLocator.AutoWireViewModel="True"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>

        <Border 
            Grid.Column="0"
            Grid.Row="0"
            Panel.ZIndex="1"
            Background="AliceBlue"
            Width="250">
            <GroupBox Header="Buttons">
                <ItemsControl
                    ItemsSource="{Binding TestButtons}"
                    ItemTemplate="{StaticResource TestButtonTemplate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter"/>
                    </ItemsControl.ItemContainerStyle>
                    <ItemsControl.Template>
                        <ControlTemplate TargetType="ItemsControl">
                            <ItemsPresenter/>
                        </ControlTemplate>
                    </ItemsControl.Template>
                </ItemsControl>
            </GroupBox>
        </Border>

                <ItemsControl 
            Grid.Row="0"
            Grid.Column="1"
            ItemsSource="{Binding CanvasItems}">
            <ItemsControl.ItemTemplateSelector>
                <slc:CanvasItemsTemplateSelector
                    MarkRectangleTemplate="{StaticResource MarkRectangleTemplate}"/>
            </ItemsControl.ItemTemplateSelector>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas 
                        Width="900" 
                        Height="1000"
                        Focusable="True"
                        RenderTransformOrigin="0.5, 0.5"
                        Background="Black"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter"/>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.Template>
                <ControlTemplate TargetType="ItemsControl">
                    <ItemsPresenter/>
                </ControlTemplate>
            </ItemsControl.Template>
        </ItemsControl>
    </Grid>
</Window>
